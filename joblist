sleep 1 && ls -l $HOME  && echo $SLURM_NODEID
sleep 1 && ls -l $HOME  && echo $SLURM_NODEID
sleep 1 && ls -l $HOME  && echo $SLURM_NODEID
sleep 1 && ls -l $HOME  && echo $SLURM_NODEID
sleep 1 && ls -l $HOME  && echo $SLURM_NODEID
sleep 1 && ls -l $HOME  && echo $SLURM_NODEID
sleep 1 && ls -l $HOME  && echo $SLURM_NODEID
sleep 1 && ls -l $HOME  && echo $SLURM_NODEID
